# Redcarpet is a markdown processor
# It's the thing that converts markdown to HTML
# GitHub uses redcarpet for conversion so we want to remain consistent
markdown: redcarpet
# The folder our website is hosted in
# Everything that comes after your website/
baseurl: /dinosaurs
